# Optional simplification backends. Install to unlock the matching algorithms in
# the viewer's "Algorithm" dropdown (they show as disabled until present):
#   pip install -r requirements-extra.txt
#
# open3d   unlocks: open3d_quadric, open3d_cluster
# pymeshlab unlocks: meshlab_quadric, meshlab_cluster
# Both are heavy and need system libs (see Dockerfile EXTRAS: libgl1, libgomp1,
# libegl1, libxkbcommon0, libdbus-1-3).
open3d>=0.18
pymeshlab>=2023.12
